# Prototype

- It is a creational design pattern that offers an interface to clone
an existing class object, without making the code dependent on class itself.
- just add a clone() interface in base, implement them in subs